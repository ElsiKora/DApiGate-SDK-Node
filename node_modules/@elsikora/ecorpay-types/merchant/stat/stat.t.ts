enum EGroupBy {
  HOUR = "hour",
	WEEK = "week",
	DAY = "day",
	MONTH = "month",
	YEAR = "year",
};

export interface IFilter {
  way?: string;
  currency?: string;
  groupBy: EGroupBy;
  dateFrom: string;
  dateTo: string;
};

export interface IResponse {
  items: [{
    data: string;
    totalCount: number;
    count: number;
    amount: number;
    conversion: number;
  }];
  count: number;
}